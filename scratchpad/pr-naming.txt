Phase 1 — P1.3: naming ceremony (Culture-style suggestions).

The rename wizards (probe / Manny / storage container) open pre-filled with a
Culture-style name suggestion from a new lexicon (src/app/lexicon.rs — original
coinages in the Iain M. Banks register, each under the 40-char Manny limit):

- Tab regenerates another suggestion (next_name_suggestion cycles the bank via
  a per-session seed);
- Enter applies, editing is free-form, Esc keeps the current name.

The API has no name-at-creation (and an assembled drone only appears ~3 h
later), so naming is always a post-hoc rename.

278 tests pass. Last of Phase 1: P1.4 (idle-manny cycling).
